spring:
  r2dbc:
    schema: classpath:schema.sql
    data: classpath:bigdata.sql
server:
  tomcat:
    accesslog:
      enabled: true
  servlet:
    session:
      cookie:
        http-only: true
        secure: true
